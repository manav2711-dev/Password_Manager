export type Hospitals = {
    id:number;                // id
    typeStyleValue:string;   // name
    remarks:string;         // cityname
    flagMessage:string;    // state name
    imgPath:string;       // address
    oldName:string;
    emailId:string;      //contact no
}