export type Doctors = {
      id: number;
   typeStyleValue: string;  //doctorname
   key:string //specialization
    dob: Date|null;
    male: string;
    qualificationId: number |null 
    bloodGroup: string;
    mobile: string|number;
    address: string;
}